<Window x:Class="StudyPlanner.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:vw="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
        xmlns:components="clr-namespace:StudyPlanner.App.Components"
        d:DataContext="{d:DesignInstance Type=components:TaskViewModel}"
        mc:Ignorable="d"
        Height="800"
        Width="1200"
        Background="White"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="NoResize">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding InitializeCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Window.Resources>
        <DataTemplate x:Key="taskTemplate">
            <Border BorderBrush="Gray" BorderThickness="0.3" CornerRadius="3" Height="40" VerticalAlignment="Center" Margin="3">
                <WrapPanel Width="480" Height="40">
                    <Button Content="Done"
                            Command="{Binding ToggleCompletedCommand}"
                            CommandParameter="{Binding}"
                            Style="{StaticResource DefaultButtonStyle}"
                            Visibility="{Binding IsToggleCompletedVisible, Converter={StaticResource BoolToVisibility}}"
                            Width="40" />
                    <Button Content="Delete"
                            Command="{Binding DeleteCommand}"
                            Style="{StaticResource DefaultButtonStyle}"
                            Visibility="{Binding IsEditModeEnabled, Converter={StaticResource BoolToVisibility}}"
                            Width="40" />
                    <Button Content="Edit"
                            Command="{Binding EditCommand}"
                            Style="{StaticResource DefaultButtonStyle}"
                            Visibility="{Binding IsDisplayMode, Converter={StaticResource BoolToVisibility}}"
                            Width="40" />
                    <Button Content="Save"
                            Command="{Binding SaveCommand}"
                            CommandParameter="{Binding ElementName=TaskDescriptionEditBox, Path=Text}"
                            Style="{StaticResource DefaultButtonStyle}"
                            Visibility="{Binding IsEditModeEnabled, Converter={StaticResource BoolToVisibility}}"
                            Width="40" />
                    <Button Content="Undone"
                            Command="{Binding ToggleCompletedCommand}"
                            CommandParameter="{Binding}"
                            Style="{StaticResource DefaultButtonStyle}"
                            Visibility="{Binding IsUndoneVisible, Converter={StaticResource BoolToVisibility}}"
                            Width="50" />
                    <TextBlock x:Name="TaskLabel"
                               Text="{Binding ShortDescription}"
                               Margin="10,10,50,10"
                               Height="20"
                               Visibility="{Binding IsDisplayMode, Converter={StaticResource BoolToVisibility}}" />
                    <TextBox x:Name="TaskDescriptionEditBox"
                             Text="{Binding ShortDescription}"
                             Margin="10,10,50,10"
                             Height="20"
                             Visibility="{Binding IsEditModeEnabled, Converter={StaticResource BoolToVisibility}}" />
                </WrapPanel>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                    <Setter TargetName="TaskLabel" Property="TextDecorations" Value="Strikethrough" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="500" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Label Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Content="{Binding GreetingText}" FontSize="25" />
        <StackPanel Grid.Row="2" Grid.Column="1">
            <TextBox Style="{StaticResource TextBoxStyle}" x:Name="DescriptionField" BorderThickness="0" VerticalAlignment="Center" />
            <Button Content="Add Task"
                    Height="30"
                    Command="{Binding AddTaskCommand}"
                    CommandParameter="{Binding ElementName=DescriptionField, Path=Text}"
                    Style="{StaticResource DefaultButtonStyle}" />
        </StackPanel>
        <ItemsControl x:Name="TasksList"
                      Grid.Row="2"
                      Grid.Column="3"
                      ItemsSource="{Binding Tasks}"
                      ItemTemplate="{StaticResource taskTemplate}"
                      Style="{StaticResource MainTaskPanelStyle}"
                      Template="{StaticResource ItemsControlTemplate}"
                      VirtualizingPanel.IsVirtualizing="True"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <vw:VirtualizingWrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <WrapPanel Grid.Column="5" Grid.Row="0" HorizontalAlignment="Right">
            <Button x:Name="MinimizeButton" Content="—" Click="MinimizeButton_Click" Style="{StaticResource MinimizeButtonStyle}" />
            <Button x:Name="CloseButton" Content="✕" Click="CloseButton_Click" Style="{StaticResource CloseButtonStyle}" />
        </WrapPanel>
    </Grid>
</Window>
